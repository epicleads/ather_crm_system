services:
  - type: web
    name: ather-crm-system-uday3
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --bind 0.0.0.0:$PORT --worker-class eventlet --workers 1
    envVars:
      - key: FLASK_ENV
        value: production
      - key: SUPABASE_URL_UDAY3
        sync: false
      - key: SUPABASE_ANON_KEY_UDAY3
        sync: false
      - key: SUPABASE_KEY_UDAY3
        sync: false
    healthCheckPath: /
    autoDeploy: true

  # Auto-assign worker using consolidated system for Uday3 branch
  - type: worker
    name: auto-assign-worker-uday3
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python auto_assign_system.py render
    envVars:
      - key: SUPABASE_URL_UDAY3
        sync: false
      - key: SUPABASE_ANON_KEY_UDAY3
        sync: false
      - key: LOG_LEVEL
        value: INFO
      - key: CHECK_INTERVAL
        value: "60"
      - key: MAX_LEADS_PER_BATCH
        value: "50"
    plan: starter
    autoDeploy: true 